datatypes xsd = "http://www.w3.org/2001/XMLSchema-datatypes"

start = script 

script = element script {title, line}

title = element title {name, text}
line = element line {name, text}

name = element name {type}

type = attribute type {text}
